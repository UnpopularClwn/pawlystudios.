# Portfolio Website — CLAUDE.md

This project folder is the only source of truth. Do not create a second project folder, duplicate
app, or experimental copy.

Before making major changes:

- Read `docs/Content Brief` and `paul-portfolio-mockup.html` for tone, copy, and visual direction.
- Read `docs/decision-records.md` — these are approved strategic decisions. Do not reopen them
  without a direct conflict.
- Read `docs/implementation-plan.md` and `docs/implementation-status.md` for current state.

After every work session:

- Update `docs/implementation-status.md` (completed / in progress / pending / blocked / needs
  input).

Rules that apply to all future work on this project:

- Do not invent business claims, metrics, testimonials, or contact information (phone, WhatsApp,
  email, partner details, client names). If real information is missing, add a clearly labeled
  placeholder and document it under "Needs Your Input."
- Paul is the primary brand and point of contact. Never position him as a Virtual Assistant or as
  looking for full-time employment.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
